---
// Basic Container component - provides horizontal padding and max-width
interface Props {
  className?: string;
}
const { className } = Astro.props;
---
<div class:list={["container mx-auto px-4 sm:px-6 lg:px-8", className]}>
  <slot />
</div>